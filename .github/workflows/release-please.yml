name: Release Please

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release-please:
    uses: groundsgg/.github/.github/workflows/release-please.yml@main
    secrets: inherit
    permissions:
      contents: write
      issues: write
      pull-requests: write
